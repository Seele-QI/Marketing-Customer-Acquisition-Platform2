appId: com.cuocuoai
productName: 招财猫
copyright: Copyright © 2026 招财猫

directories:
  output: release
  buildResources: build

files:
  - "electron/dist-electron/**/*.js"
  - "package.json"
  - "!**/*.map"
  - "!**/__tests__/**"
  - "!**/*.test.ts"

asar: true
asarUnpack:
  - "node_modules/node-machine-id/**"
  - "node_modules/electron-log/**"

extraResources:
  - from: "resources"
    to: "."
    filter:
      - "python/**"
      - "ffmpeg/**"
      - "runtime/**"
      - "next-standalone/**"
      - "bgm/**"
      - "main.py"
      - "routes/**"
      - "scripts/**"
      - ".env"

extraMetadata:
  main: electron/dist-electron/main.js
  version: 1.3.8

releaseInfo:
  releaseNotes: 修复 GEO 内容矩阵生成成功却误报失败的问题，提升长耗时生成结果返回稳定性。

afterPack: scripts/after-pack.cjs

win:
  target:
    - target: nsis
      arch:
        - x64
  icon: build/icon.ico
  artifactName: ${productName}-Setup-${version}.${ext}
  # 跳过 rcedit/winCodeSign 下载（内网/无法访问 GitHub 时 dist:win 会超时失败）
  signAndEditExecutable: false

# Mac：分别产出 arm64 / x64 DMG（不做 universal lipo；next-standalone 含单架构原生依赖）
# 运行时仍可按 process.arch 选择 resources/runtime/darwin-*
mac:
  target:
    - target: dmg
      arch:
        - arm64
        - x64
  icon: build/icon.icns
  category: public.app-category.business
  artifactName: ${productName}-${version}-mac-${arch}.${ext}
  identity: null
  hardenedRuntime: false
  gatekeeperAssess: false

dmg:
  artifactName: ${productName}-${version}-mac-${arch}.${ext}

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: 招财猫
  uninstallDisplayName: 招财猫 ${version}
  # 默认安装目录使用纯 ASCII，避免中文路径导致 node-machine-id 等原生模块加载失败
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  include: "build/installer.nsh"
  installerLanguages:
    - zh_CN
  deleteAppDataOnUninstall: false

# 国内 OSS/CDN generic 源（构建时生成 latest.yml；运行时可用 UPDATE_FEED_URL 覆盖）
# 发版上传：pnpm release:upload-oss —— 见 docs/deploy/DESKTOP-UPDATE-OSS.md
publish:
  provider: generic
  url: https://mcap-desktop-releases.oss-cn-beijing.aliyuncs.com/releases/
