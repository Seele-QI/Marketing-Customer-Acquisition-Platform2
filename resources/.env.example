# 招财猫桌面安装包 resources/.env 模板（由 scripts/copy-packaged-env.mjs 生成）
# 勿在此填写 API Key — 登录后由云端 POST /api/config/sync 下发

CLOUD_API_URL=https://mcap-cloud-api.preview.aliyun-zeabur.cn

# electron-updater / 强更下载页（与 OSS releases 前缀一致）
UPDATE_FEED_URL=https://mcap-desktop-releases.oss-cn-beijing.aliyuncs.com/releases/
CENTRAL_UPDATE_URL=https://mcap-desktop-releases.oss-cn-beijing.aliyuncs.com/releases/

# ── 以下 Key 由云端 sync 下发（与 zhongtai-cloud/contracts/CONFIG_KEYS.md 对齐）──
# NewAPI 三选：NEWAPI_* / NEWAPI_SECONDARY_* / NEWAPI_TERTIARY_*
# Seedance 三选：SEEDANCE_PRIMARY_* / SEEDANCE_SECONDARY_* / SEEDANCE_TERTIARY_*
# 豆包推理：ARK_API_KEY、ARK_CHAT_MODEL、ARK_BASE_URL（见 lib/llm/ark-client.ts）
# 兼容别名：SEEDANCE_API_KEY、SEEDANCE_BASE_URL、XINGHE_API_KEY、XINGHE_BASE_URL
# 数字人创作超时：DH_V2_TASK_TIMEOUT、DH_V2_SEGMENT_POLL_TIMEOUT（默认 3000 秒 = 50 分钟）
